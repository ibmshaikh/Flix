<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibm.contentupload">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Series_Play"></activity>
        <activity android:name=".Total_Episode" />
        <activity android:name=".Movies_Play" />
        <activity android:name=".Total_Seasons" />
        <activity android:name=".DashBoard" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Upload_series" />
        <activity android:name=".Upload_Movies" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>